new CR implemented

code stash partially implemented code
 change one  tocheck push
 change two = modified 

change 4=change added on remote  by other

change 3 -> at local hh
change 4->file3 is updated for change3 and arrow added for all
